# SPECs de Backend

Specs específicas de implementação do backend (Java/Spring Boot) que não
pertencem a um laboratório específico nem à arquitetura geral (que fica em
`specs/architecture/`). Vazio até o início da Fase 1 — nenhuma SPEC de
backend foi criada ainda.
